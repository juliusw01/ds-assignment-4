apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-database.fullname" . }}-test-connection"
  labels:
    {{- include "product-database.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-database.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
